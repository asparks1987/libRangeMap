# Legacy Sources

This directory contains pre-alpha experimental code that is not part of the Alpha v1 release gate.

`librangemap.h` is preserved for historical reference. It defaults to `[0.0, 1.0]` and includes character behavior that does not satisfy the current Alpha v1 spec.

C++ support is deferred until it can be updated, documented, and tested against the language-agnostic compliance spec.
