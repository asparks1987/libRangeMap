# Security

`libRangeMap` is designed for local and offline data-normalization workflows.

The core SDK does not collect telemetry, make network calls, download models, upload data, or log datasets by default.

Please report security concerns through the project's GitHub repository.
